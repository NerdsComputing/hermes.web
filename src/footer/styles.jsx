import { makeStyles } from '@material-ui/core'

export const useStyles = makeStyles({
  paragraph: { color: 'white' },
  footer: { backgroundColor: 'blue', height: '11.5vh' },
  gridHeight: { height: '100%' },
  divHeight: { height: '80vh' },
})
