import { makeStyles } from '@material-ui/core'

const image = {
  filter: 'invert(100%) sepia(0%) saturate(0%) hue-rotate(93deg) brightness(103%) contrast(103%)',
  height: '17px',
  marginBottom: '-3px',
}

export const useStyles = makeStyles({ image })
