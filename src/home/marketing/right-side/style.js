import { makeStyles } from '@material-ui/core'

const floatImage = { float: 'right', paddingLeft: '15px', paddingBottom: '10px' }

const display = { display: 'block' }

const box = { marginLeft: '10px', paddingRight: '10px' }

const width = { maxWidth: '100%' }

export const useStyles = makeStyles({ floatImage, display, box, width })
