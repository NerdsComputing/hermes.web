import { makeStyles } from '@material-ui/core'

const title = {
  fontSize: '2.8vw',
  paddingTop: '2vw',
  textAlign: 'center',
  justifyContent: 'center',
}

export const useStyles = makeStyles({ title })
