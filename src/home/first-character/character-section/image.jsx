import React from 'react'
import { Box } from '@material-ui/core'

import { useStyles } from 'home/first-character/character-section/use-style'

export const Image = () => <Box classes={useStyles()} />
