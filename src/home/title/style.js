import { makeStyles } from '@material-ui/core'

const head = {
  fontSize: '2.8vw',
  paddingTop: '2vw',
  textAlign: 'center',
  justifyContent: 'center',
}

export const useStyle = makeStyles({ head })
