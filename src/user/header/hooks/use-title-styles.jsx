import { makeStyles } from '@material-ui/core'

const root = {
  textAlign: 'center',
  fontSize: '2vw',
  backgroundColor: 'transparent',
  boxShadow: 'none',
}

export const useTitleStyles = makeStyles({ root })
