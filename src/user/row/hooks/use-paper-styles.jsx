import { makeStyles } from '@material-ui/core'

const root = {
  width: '19vw',
  paddingTop: '1vh',
  paddingBottom: '1vh',
  textAlign: 'center',
  fontSize: '1vw',
}

export const usePaperStyles = makeStyles({ root })
