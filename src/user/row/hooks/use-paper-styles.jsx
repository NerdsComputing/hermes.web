import { makeStyles } from '@material-ui/core'

const root = {
  width: '19.5vw',
  paddingTop: '1vh',
  paddingBottom: '1vh',
  textAlign: 'center',
  fontSize: '1vw',
  backgroundColor: 'transparent',
  boxShadow: 'none',
}

export const usePaperStyles = makeStyles({ root })
