import { makeStyles } from '@material-ui/core'

const root = {
  paddingLeft: 72,
  height: '50px',
  margin: '0 auto',
}

const phone = {
  paddingLeft: 16,
  paddingTop: 10,
  height: '50px',
}
export const useStyles = makeStyles({ root, phone })
