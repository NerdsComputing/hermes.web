import { useStyles } from 'detections/chart/style'

export const useMessageProps = () => (
  {
    container: 'true',
    justify: 'center',
    direction: 'column',
    alignItems: 'center',
    classes: useStyles(),
  })

