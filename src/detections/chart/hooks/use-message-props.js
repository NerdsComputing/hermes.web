import { useStyles } from 'detections/chart/style'

export const useMessageProps = () => (
  {
    justify: 'center',
    direction: 'column',
    alignItems: 'center',
    classes: useStyles(),
  })

