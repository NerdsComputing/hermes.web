import { useStyles } from 'cameras/list/style'

export const useMessageProps = () => ({
  container: true,
  justify: 'center',
  direction: 'column',
  alignItems: 'center',
  classes: useStyles(),
})
